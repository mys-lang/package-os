from os import nproc

func main():
    print("nproc():              ", nproc())
    print("nproc(all=True):      ", nproc(all=True))
    print("nproc(ignore=1):      ", nproc(ignore=1))
    print("nproc(ignore=nproc()):", nproc(ignore=nproc()))
